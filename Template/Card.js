const app = Vue.createApp({
    data() {
        return {};
    },
    methods: {
        add(id) {},
        reduce(id) {},
        delete(id) {},
        pay() {},
    },
});
app.mount("cart_formId");
